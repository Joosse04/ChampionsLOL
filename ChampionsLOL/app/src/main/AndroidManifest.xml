<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.izv.championslol">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChampionsLOL">
        <activity
            android:name=".view.activity.EditChampionActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.CreateChampionActivity"
            android:exported="false"
            android:parentActivityName=".view.activity.ChampionActivity"
            />
        <activity
            android:name=".view.activity.ChampionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>